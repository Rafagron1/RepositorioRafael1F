using UnityEngine;

public class Parte3Ex9 : MonoBehaviour
{
    int a = 10;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    public void Ex9()
    {
        print("Exercício 9\n" + "Valor base: " + a + "\n" + "Dobro= " + (a * 2) + " Metade= " + (a / 2));
    }
}
