using UnityEngine;

public class Parte2Ex7 : MonoBehaviour
{
    public int a;
    public int b;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    public void Ex7()
    {
        print("Exercício 7\n" + (a + b));
    }
}
